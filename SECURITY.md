# Security

DassieDrop is designed for trusted local networks.

- Designed for trusted LANs.
- Do not expose DassieDrop directly to the internet.
- If you need remote access, put it behind a reverse proxy with TLS and use an access code.
- Files expire after 24 hours by default.
